# Session 11 - Benchmark Part A vs BnB (2025-11-29)

## Description
Kick off benchmarking work for Part A NF4 kernel versus standard bitsandbytes baseline to pursue the +3 FSDP bonus.

## Actions
- Checked repo status and active branch; created work branch `bench/part-a-bnb`.
- Queried bd ready queue and created issue `unsloth-challenges-r7m` (priority 1) for benchmarking; marked in_progress.
- Attempted `npx -y @morphllm/morphmcp warp_grep` per instructions; CLI errored with ENOENT for `warp_grep` path (no results).
- Reviewed `journal/current_state.md` to refresh challenge status and prior findings.
- Skimmed `tests/test_part_a_fsdp_paths.py` and `challenge_b_train_with_part_a.py` to understand current Part A FSDP integration and benchmark hooks.
- Installed `numpy` to clear pytest warning; `python -m pytest tests/test_part_a_fsdp_paths.py` clean (5 tests).
- Recorded failed Oracle browser attempt for benchmark guidance (session not logged in).
- Ran Modal BnB baseline (`modal_challenge_b.py::run_part_a_kernel_training --no-use-part-a --max-steps 5`, app `ap-jlnjfaoYpKtz28v66lagE0`): training time 162.09s, loss ~6.9.
- Ran Modal Part A run without cache (`--use-part-a --max-steps 5`, app `ap-qOac72pFSi2fTtJvjg1OWf`): training time 266.33s, part_a_calls=13440, part_a_time=200.73s, losses ~14 (worse than BnB).
- Attempted Part A with gather cache default-on (app `ap-jXv1Vo3SxW3fwRmXRyomlq`); aborted on first step with CUDA OOM (112MB alloc) while caching full packed bytes; reverted default to cache-off and guarded with unit test.
- Tried `pre-commit install` but git `core.hooksPath` points to existing `git_hooks/pre-commit`; left config unchanged (hook already runs tests/test_part_a_fsdp_paths.py).
- Added scatter-path reshape fix and modal env toggles; new unit test for flat scatter input.
- Modal Part A scatter-only run after fix (reacquire/gather off, app `ap-plgw0ijiBY9cah0cCEfhqH`): training time 181.96s, loss 13.43, part_a_calls=13440, bnb_calls=9600 (shape mismatches force fallback); still slower and poor loss.
- Modal Part A scatter-only earlier (pre-fix, app `ap-pguabUVvocoBAWzXfTexkI`): training time 192.15s, loss 6.90 due to full BnB fallback; per-call speedup 0.02x.
- Modal Part A scatter-only with cache-off but old scatter logic (app `ap-jXv1Vo3SxW3fwRmXRyomlq`): OOM when gather cache enabled; documented.

## Notes
- morphmcp warp_grep command currently fails with "ENOENT: no such file or directory, stat 'warp_grep'"; needs follow-up if required.
- Part A all-gather path is significantly slower than BnB in FSDP2 (5-step run: 266s vs 162s) and shows much higher loss, indicating integration still not viable for +3 bonus.
- Caching full packed bytes to avoid repeated all-gathers is infeasible on 2xT4 due to OOM (~112MB alloc failure on first step).
