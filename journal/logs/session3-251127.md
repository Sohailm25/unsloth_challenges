## description of what was run, for what purpose
- Continue NF4 kernel work: fix dependency pins, rebuild venv for Modal CLI, run Modal tests/benchmarks, and debug kernel mismatches producing NaNs.

### Actions
- Recreated `.venv` with Python 3.11 (Modal incompatible with 3.14) and reinstalled `modal` CLI.
- Removed `xformers` from Modal images; added PYTHONPATH injection for mounts; ensured peft/transformers/bitsandbytes pins via Oracle guidance.
- Added Oracle research log `research/oracle_find-a-torch-triton-bitsandbyt.md` capturing torch/triton/xformers compatibility and minimal pin set.
- Refactored fast_dequantize handling: introduce `_call_fast_dequantize` wrapper for unsloth/peft signatures; updated `unsloth_dequantize` and debug utilities to use it.
- Modified NF4 kernel: constexpr shift params, added bounds masks for absmax/absmax2, removed autotune (fixed BLOCK_SIZE=256), strided stores, mlp_dequantize no longer transposes.
- Multiple Modal runs:
  - `modal_challenge_a.py::run_tests` fails: your kernel outputs NaNs; torch.compile comparison fails.
  - `modal_debug_nf4.py::debug_case` shows kernel NaNs; manual block reconstruction matches reference while kernel diverges (e.g., block_start 512/3648).
  - `debug_block` confirms first block matches reference; later blocks diverge.
- Updated debug helpers to print mismatch block details and manual reconstruction; optional_flags test now passes after shape change.

### Open issues
- Kernel still produces NaNs on many blocks despite correct manual reconstruction; root cause likely in Triton kernel execution rather than algorithm. Benchmarks not run yet due to correctness failure.
