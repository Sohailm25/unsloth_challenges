# Session 12 - Part A shape-parity attempt (2025-11-29)

## Description
Implemented Oracle guidance to match BnB output orientation for Part A under FSDP2 and re-ran scatter-only benchmarks. Runs still fail early with `AttributeError: 'NoneType' object has no attribute 'to'` from BnB matmul (our dequant returned None). Documented failures.

## Actions
- Added NCCL defaults and output buffer reuse in `challenge_b_train_with_part_a.py` per Oracle advice.
- Reworked scatter to support column-scatter (transpose) using BnB reference shape; oriented Part A outputs via `_orient` helper; added defensive BnB None guard.
- Expanded unit tests to cover scatter shape cases; pytest green (7 tests).
- Ran Modal scatter-only benchmarks on 2xT4 (reacquire/gather off):
  - `2025-11-29-part-a-shape2.log` (app `ap-04h3oDWChjV4Wy0yEDOb0D`): failed step 0, `NoneType` from dequant.
  - `2025-11-29-part-a-shape3.log` (app `ap-XkDoCC60xvILoHiEJsDXUT`): same failure.
  - `2025-11-29-part-a-shape4.log` (app `ap-6c4ijTS242DS6fmugJMo3K`): same failure.
- Oracle run completed; applied recommended orientation changes (transpose to match BnB, column scatter when needed, cache reuse, NCCL toggles).
- Committed fixes and pushed branch `bench/part-a-bnb` (ahead by 2 after today: `13d0978`, `7fe0cd7`).

## Notes
- Despite orientation changes, Part A dequant still returns None in practice for FSDP2 scatter path, leading to early crash inside BnB matmul. Need deeper investigation into why `_ORIGINAL_BNB_DEQUANT` or patched path returns None (instrumentation pending).
- Older untracked research files remain untouched.
